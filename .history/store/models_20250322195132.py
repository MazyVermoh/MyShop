from django.db import models

# Здесь можно добавлять модели, например:
# class Product(models.Model):
#     name = models.CharField(max_length=200)
#     price = models.DecimalField(max_digits=8, decimal_places=2)
#     ...